pipeline {
    agent any

    stages {
        
        stage("Verify 👀") {
            steps {
                sh "git -version"
                sh "java -version"
                sh "mvn -version"
                sh "npm -version"
            }
        }
        // tests

        stage("Test 🧪") {
            steps {
                sh "mvn test"
            }
        }

        stage("Package 📦") {
            steps {
                sh "mvn compile"
                sh "npm install"
            }
        }

        stage("Deliver 🚚") {
            steps {
                sh "scripts/deploy.sh"
            }
        }

        stage("Start 🚀") {
            steps {
                sh "scripts/start.sh"
            }
        }
    }
}